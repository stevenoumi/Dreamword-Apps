import { Box } from "@mui/material";

export const Footer = () => {
  return (
    <Box
      sx={{
        width: "100%",
        height: "auto",
        backgroundColor: "primary",
        paddingTop: "1rem",
        paddingBottom: "1rem",
      }}
    >
      
    </Box>
  );
};

export default Footer;